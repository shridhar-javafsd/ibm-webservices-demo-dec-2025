// What is this export? 
// Calc3.js

const addNums = (a, b) => {
    console.log("Addition:", a + b);
};

 const subNums = (a, b) => {
    console.log("Subtraction:", a - b);
};

 const divNums = (a, b) => {
    console.log("Division:", a / b);
};

export default {addNums, subNums, divNums};
